# 모듈이 다섯이라 서로 독립인 것들(checkstyle, 모듈별 test)은 겹쳐 돌릴 수 있다.
org.gradle.parallel=true

# 빌드 캐시는 Gradle User Home(caches/build-cache-1)에 쌓이고, CI의 setup-gradle이 그
# 디렉터리를 통째로 캐시한다. main에서 채운 컴파일·checkstyle 결과를 PR이 그대로 재사용해,
# 테스트만 고친 PR은 main 소스를 다시 컴파일하지 않는다.
org.gradle.caching=true
