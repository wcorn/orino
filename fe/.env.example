# dev에서는 /api로 두면 vite proxy(localhost:8080)를 거친다.
# prod 빌드 시에만 절대 URL을 지정 (예: https://api.orino.dev/api).
VITE_API_URL=/api

# Grafana Faro Web SDK — 비워두면 Faro 초기화 생략
# prod 빌드 시 telemetry 수집기 URL과 API 키를 주입
VITE_FARO_URL=
VITE_FARO_KEY=
