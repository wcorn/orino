/**
 * 런타임 캐시 이름.
 *
 * <p>서비스워커가 쓰는 이름과 앱이 읽고 비우는 이름은 <b>같아야 한다</b>. 양쪽에 문자열을
 * 따로 적어두면 한쪽만 고쳤을 때 화면은 "저장된 것 없음"이라 하고 캐시는 계속 쌓인다.
 * 그래서 상수를 여기 하나만 두고 둘 다 가져다 쓴다.
 */
export const TRAVEL_CACHE = "travel-api";
