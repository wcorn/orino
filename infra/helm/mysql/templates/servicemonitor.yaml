apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mysql
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: mysql
      component: metrics
  endpoints:
    - port: metrics
      interval: 30s
