apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: be
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: be
  endpoints:
    - port: actuator
      path: /prometheus
      interval: 30s
