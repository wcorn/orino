apiVersion: v1
kind: Service
metadata:
  name: thanos-compactor
  labels:
    app.kubernetes.io/name: thanos
    app.kubernetes.io/component: compactor
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9090
      targetPort: http
  selector:
    app.kubernetes.io/name: thanos
    app.kubernetes.io/component: compactor
