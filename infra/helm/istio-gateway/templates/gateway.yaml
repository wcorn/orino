apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: orino-gateway
  namespace: istio-ingress
spec:
  selector:
    istio: gateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "orino.dev"
        - "*.orino.dev"
