{{- /*
공개 표면(s.orino.dev)의 rate limit. #1237 · Epic #1246.

기존 ratelimit-be는 api.orino.dev vhost에만 걸려 있어, 그대로 두면 새 공개 표면이
무방비로 열린다(아키텍처 §6). local_ratelimit HTTP 필터 자체는 ratelimit-be가 GATEWAY
컨텍스트에 이미 INSERT_BEFORE로 넣어 두었으므로, 여기서는 vhost별 설정만 얹는다.

vhost 이름은 "<host>:<Gateway 서버 포트>"다. 이 게이트웨이는 443 서버 하나뿐이므로
s.orino.dev:443이다 — :80으로 적으면 매치가 되지 않아 필터가 조용히 아무 일도 하지 않는다.
*/ -}}
apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: ratelimit-shortlink
  namespace: istio-ingress
spec:
  workloadSelector:
    labels:
      istio: gateway
  configPatches:
    - applyTo: HTTP_ROUTE
      match:
        context: GATEWAY
        routeConfiguration:
          vhost:
            name: s.orino.dev:443
      patch:
        operation: MERGE
        value:
          typed_per_filter_config:
            envoy.filters.http.local_ratelimit:
              "@type": type.googleapis.com/udpa.type.v1.TypedStruct
              type_url: type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
              value:
                stat_prefix: http_local_rate_limiter
                # vhost 전체가 나눠 쓰는 버킷이다(IP별이 아니다). 사람이 링크를 눌러 여는
                # 속도와는 자릿수가 다르고, 열거에는 한참 모자란다 — 3,355만 개를 분당 300개로
                # 훑으면 200년이 넘는다. 방어의 본체는 응답이 아무것도 알려주지 않는 것이고
                # (명세 §7.1) 이건 그 위의 보조다.
                token_bucket:
                  max_tokens: 300
                  tokens_per_fill: 300
                  fill_interval: 60s
                filter_enabled:
                  runtime_key: local_rate_limit_enabled
                  default_value:
                    numerator: 100
                    denominator: HUNDRED
                filter_enforced:
                  runtime_key: local_rate_limit_enforced
                  default_value:
                    numerator: 100
                    denominator: HUNDRED
                response_headers_to_add:
                  - append_action: OVERWRITE_IF_EXISTS_OR_ADD
                    header:
                      key: X-RateLimit-Limit
                      value: "300"
